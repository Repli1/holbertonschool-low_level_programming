#include <stdio.h>
/**
 *  * main - Entry point
 *   *
 *    * Description: Program that prints the following line
 *     *
 *      * Return: Always 0 (Success)
 *       */
int main(void)
{
		int c = 'a';

			for (c = 0; c <= 9; c++)
						putchar(c);
				putchar('\n');
					return (0);
}
