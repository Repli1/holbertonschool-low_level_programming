#include "main.h"
/**
 * rev_string - Entry point
 *
 * @s: It's a string!
 *
 * Return: Always 0 (Success)
 */

void rev_string(char *s)
{
	int n;

	double rev[n];

	for(n = 0; s[n] == '\0'; n++)
		rev[n] = s[(n - 1)];

	*s = rev[a];
}
