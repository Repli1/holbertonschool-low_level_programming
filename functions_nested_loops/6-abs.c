#include "main.h"
/**
 * print_alphabet_x10 - Entry point
 *
 * @int: Variable (Confusing)
 *
 * Return: Always 0 (Success)
 */

int _abs(int)
{
	if (int > 0)
		return (int);
	else if (int == 0)
		return (0);
	else
		return (-int);
}
